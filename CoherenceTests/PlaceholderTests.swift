import XCTest

/// Phase 0 placeholder so the test target has a source file and the scheme's
/// test action is wired up. Phase 3 adds the real CoherenceEngine tests.
final class PlaceholderTests: XCTestCase {
    func test_phase0_buildsAndRuns() {
        XCTAssertTrue(true)
    }
}
